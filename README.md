<h1 align="center">ГПО БПЛА 🚀👉🇺🇦😎</h1>

ТУСУР, групповой проект "Моделирование полётов БПЛА"

<h1 align="center">Краткая инструкция по Git и GitHub</h1>

Гайд по установке и настройке `git` окружения для работы, список команд для работы, а также правила работы с этим репозиторием и его ветками.

## Содержание

Читаем по порядку:

1. Скачивание и первичная настройка Git For Windows
2. Настройка SSH для манипуляций с репозиторием на GitHub
3. Клонирование репозитория на свой комьютер
3. Основные git команды в терминале
4. Правила работы с репозиторием и ветками (ОБЯЗАТЕЛЬНО)
5. *\* Дополнительные git команды (по желанию)*

## Скачивание Git For Windows

Скачиваем сборку ***Git For Windows [[СКАЧАТЬ]](https://drive.google.com/file/d/1Z-KpbOlPzJq3uQU6l58Iei2_wLfjVaCd/view?usp=drive_link)***

- Распаковываем архив
- Делаем ярлык для запуска
- Запускаем ярлык

Далее, вводим:

```shell
which git 
# Вывод должен быть
# > /mingw64/bin/git

git --version
# Вывод должен быть
# > git version 2.53.0.windows.1
```

Если вывод совпал, начинаем первичную настройку.

### Первичная настройка Git For Windows

В первую очередь нужно задать имя и email. Email нужен чтобы GitHub отображал ваш аккаунт на странице репозитория (вкладка **Contributors**)

```shell
# Задаем свое имя в формате "Ivanov Ivan"
git config --global user.name "Ivanov Ivan"

# Задаем почту, которую указывали при регистрации на GitHub
git config --global user.email "example@mail.com"
```

```shell
# Если хотим проверить имя и почту
git config --global user.name && git config --global user.email
```

## Настройка SSH для манипуляций с репозиторием на GitHub 

Настраивать соединение с GitHub по SSH нужно чтобы иметь возможность работать с удаленным репозиторием через терминал. SSH ключ - это аналог связки `логин + пароль` в браузере, только для терминала.

Все просто, нужно будет:

- Сгенерировать ssh ключ
- Добавить ключ на GitHub:
    1. Скопировать содержимое публичного ключа на компьютере
    2. Открыть настройки аккаунта GitHub и вставить ключ
- Провести первичное "рукопожатие" с GitHub

### Генерация ssh ключа

```shell
# Создаем новый ssh ключ 
ssh-keygen -t ed25519

# Вопрос, какую директорию и название задать ключу.
# По умолчанию, это С:\Users\<username>\.ssh\id_ed25519.
#
# Жмем Enter, чтобы согласится на вариант по умолчанию
> Enter file in which to save the key (/c/Users/<username>/.ssh/id_ed25519):

# Если директории не было, она будет создана.
> Created directory '/c/Users/<username>/.ssh'.

# Вопрос, нужно ли зашифровать файл ключа (физически на диске)
# на основе пароля. Лучше не шифровать, иначе придется вводить пароль 
# при любой манипуляции с удаленными репозиториями.
# 
# Жмем Enter, чтобы отказаться от шифровки
> Enter passphrase for "/c/Users/<username>/.ssh/id_ed25519" (empty for no passphrase):

# Введите пароль снова. Он отсутствует, жмем Enter
> Enter same passphrase again:

# Ключ успешно создан
> Your identification has been saved in /c/Users/bubsage/.ssh/id_ed25519
> Your public key has been saved in /c/Users/bubsage/.ssh/id_ed25519.pub
```

### Как скопировать содержимое публичного ключа

Публичный ключ по умолчанию находится в директории `С:\Users\<username>\.ssh\` и имеет имя `id_ed25519.pub`.

> В этой же директории лежит другой файл, `id_ed25519` - это секретный ключ, его не трогаем, он нужен для SSH аутентификации.

```shell
# Открываем публичный ключ в Notepad
$(cd /c/Users/$USERNAME/.ssh/ && notepad id_ed25519.pub)
```

Копируем содержимое файла.

### Куда вставлять ключ настройки аккаунта GitHub